const polonlyzeCore = async ({exchange, keyFile, lending, margin, nonceOffset, startDatetime, stopDatetime}) => {
  const retVal = {}
  if(exchange) {
    retVal.exchange = {}
  }
  return retVal
}

module.exports = polonlyzeCore
